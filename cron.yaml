version: 1
cron:
 - name: "last-day-splitted-stats"  # required - unique across all entries in this file
   url: "/last-day-splitted-stats"              # required - does not need to be unique
   schedule: "0 */12 * * *"    # required - does not need to be unique
 - name: "update-percentile"  # required - unique across all entries in this file
   url: "/update-percentile"              # required - does not need to be unique
   schedule: "0 */12 * * *"    # required - does not need to be unique
